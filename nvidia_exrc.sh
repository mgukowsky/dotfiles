# Shell configuration specific to Nvidia GPUs
#
# Prefix a vulkan executable invocation to use it with a specific driver
# e.g. VK_NV vkcube
alias VK_NV='VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/nvidia_icd.json'
